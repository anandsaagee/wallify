/**
 * store.js — Wallify Store
 * Shared store utility: card creation, shuffle, scroll reveal binding.
 * Each HTML page handles its own DOMContentLoaded initialization.
 */

console.log("STORE LOADED");
